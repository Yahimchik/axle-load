package com.mehatronics.axle_load.domain.usecase;

/**
 * UseCase для сохранения таблицы калибровки в устройство.
 *
 * <p>Инкапсулирует бизнес-логику, связанную с обработкой и передачей таблицы
 * калибровки на устройство.</p>
 */
public interface SaveCalibrationTableUseCase {

    /**
     * Выполняет сохранение таблицы калибровки.
     *
     * @return Код выполнения:
     * <ul>
     *     <li>{@code 0} — если операция выполнена успешно</li>
     *     <li>{@code >0} — если произошла ошибка, например, недопустимый множитель</li>
     * </ul>
     */
    int execute();
}
